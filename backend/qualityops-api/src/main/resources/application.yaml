server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qualityops_db
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: qualityops-api